# Código de acceso que compartes con los participantes
WORKSHOP_ACCESS_CODE=FLOCI-2026-ABCD

# Secreto para firmar JWT (se genera automáticamente si no se define)
JWT_SECRET=cambiar-en-produccion

# Puerto del backend
PORT=3000

# Máximo de sesiones concurrentes
MAX_SESSIONS=50

# Puerto base para contenedores Floci (se asignan secuencialmente)
FLOCI_BASE_PORT=4566

# Duración máxima de sesión en horas
SESSION_DURATION_HOURS=4
